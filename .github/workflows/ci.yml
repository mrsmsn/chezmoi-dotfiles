name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint shell scripts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: bash syntax check
        run: bash -n install.sh
      - name: Install zsh
        run: sudo apt-get update && sudo apt-get install -y zsh
      - name: zsh syntax check
        run: |
          for f in home/dot_zshrc home/private_dot_config/zsh/*.zsh; do
            zsh -n "$f"
          done
      - name: shellcheck
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: .
          # .tmpl は chezmoi テンプレ、nix/ は Nix 式なので shellcheck の対象外。
          ignore_paths: home nix

  flake-check:
    # macOS runner はコスト都合で省略。darwinConfigurations は ubuntu でも
    # eval されるのでモジュール評価エラーはここで検出できる。
    name: nix flake check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
      - name: nix flake check
        working-directory: nix
        env:
          USER: runner
        # --no-build を付けない: hunk (bun2nix) の eval が IFD を要求するため、
        # 付けると 'path is not valid' で落ちる。checks 出力は無いので
        # 外しても IFD の実現以外にビルドは発生しない。
        run: nix flake check --impure

  build-configs:
    # darwinConfigurations.default は macOS runner が要るので build しない。
    name: build ${{ matrix.label }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: homeConfigurations.linux
            attr: homeConfigurations.linux.activationPackage
          - label: homeConfigurations.wsl
            attr: homeConfigurations.wsl.activationPackage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
      - name: nix build
        working-directory: nix
        env:
          USER: runner
        run: nix build --impure --no-link --print-build-logs ".#${{ matrix.attr }}"

  template-variants:
    name: .chezmoi.toml.tmpl variant detection
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
      - name: Install chezmoi
        run: nix profile install nixpkgs#chezmoi
      - name: Install bats
        run: sudo apt-get update && sudo apt-get install -y bats
      - name: Run template_variants bats
        # PROJECT_ROOT は Podman 用 /repo fallback を回避するための上書き。
        env:
          PROJECT_ROOT: ${{ github.workspace }}
        run: ./ci/run-bats.sh ci/test/template_variants.bats

  template-shellcheck:
    name: shellcheck rendered shell templates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
      - name: Install chezmoi and shellcheck
        run: nix profile install nixpkgs#chezmoi nixpkgs#shellcheck
      - name: Render and shellcheck each variant
        # run_onchange_20-* は `{{ output "git" ... }}` を使うので単発 render が
        # 困難 (E2E は bootstrap-linux job)。run_onchange_30 は chezmoi data を
        # 消費するので別途 test config を渡す。実体は ci/template-shellcheck.sh
        # に集約してあるので local と CI の乖離を防ぐ。
        run: ./ci/template-shellcheck.sh

  git-profile:
    name: gitdir-based work プロファイル切替の E2E
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
      - name: Install chezmoi
        run: nix profile install nixpkgs#chezmoi
      - name: Install bats
        run: sudo apt-get update && sudo apt-get install -y bats
      - name: Run git-profile bats
        env:
          PROJECT_ROOT: ${{ github.workspace }}
        run: ./ci/run-bats.sh ci/test/git_profile.bats

  bats-unit:
    name: bats unit + lite E2E
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install bats and chezmoi
        run: |
          sudo apt-get update
          sudo apt-get install -y bats
          sh -c "$(curl -fsLS get.chezmoi.io)" -- -b /usr/local/bin
      - name: Run bats unit/E2E suite
        env:
          PROJECT_ROOT: ${{ github.workspace }}
        run: |
          ./ci/run-bats.sh \
            ci/test/install_unit.bats \
            ci/test/local_ci_hook.bats \
            ci/test/nix_tree_hash.bats \
            ci/test/install_e2e.bats \
            ci/test/envrcs.bats

  bootstrap-linux:
    name: End-to-end bootstrap on Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run install.sh against the local checkout
        # nix-daemon は user-level ~/.config/nix/nix.conf を読まないので、token を
        # /etc/nix/nix.conf に書き込めるよう install.sh に forward する。
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./install.sh "file://${GITHUB_WORKSPACE}"
      - name: Persist Nix paths for subsequent steps
        run: |
          echo "/nix/var/nix/profiles/default/bin" >> "$GITHUB_PATH"
          echo "$HOME/.nix-profile/bin" >> "$GITHUB_PATH"
      - name: Verify variant is "linux"
        run: |
          variant=$(chezmoi data | jq -r .variant)
          echo "variant=${variant}"
          test "${variant}" = "linux"
      - name: Second apply is idempotent
        run: |
          chezmoi apply
          status=$(chezmoi status)
          if [[ -n "${status}" ]]; then
            echo "Dirty after second apply:" >&2
            echo "${status}" >&2
            exit 1
          fi
      - name: Smoke test that zsh sources .zshrc cleanly
        # HERDR_PANE_ID=fake で herdr_automatically_attach_session を non-TTY
        # 環境でも早期 return させる。
        run: HERDR_PANE_ID=fake zsh -i -c 'exit 0'
      - name: Verify LazyVim bootstraps headlessly
        # `Lazy! sync` の `!` で同期実行を強制し、lazy.nvim clone + LazyVim 本体 +
        # 依存プラグイン群の展開を 1 ステップで完了させる。
        run: |
          timeout 600 nvim --headless "+Lazy! sync" +qa
          test -d "${HOME}/.local/share/nvim/lazy/lazy.nvim"
          test -d "${HOME}/.local/share/nvim/lazy/LazyVim"
