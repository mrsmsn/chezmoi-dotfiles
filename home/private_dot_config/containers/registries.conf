# podman/skopeo が短縮イメージ名 (例: `ubuntu:24.04`) を解決するときの
# 検索レジストリ。Containerfile は完全修飾名を使うのでビルドには不要だが、
# 対話的な `podman run ubuntu` (`alias docker=podman`) を通すために置く。
unqualified-search-registries = ["docker.io"]
